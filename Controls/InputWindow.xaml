<Window x:Class="Spotlight.InputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Spotlight"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        Background="Black"
        Padding="10"
        Height="150"
        Width="400"
        KeyDown="Window_KeyDown"
        Title="InputWindow">
    <StackPanel Orientation="Vertical">
        <TextBlock Text="{Binding DisplayText}" />
        <TextBox Height="40" VerticalContentAlignment="Center" Foreground="#FFB5B8C1" KeyDown="InputTextbox_KeyDown" x:Name="InputTextbox" />
        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
            <Button x:Name="OkButton" Click="OkButton_Click">Okay</Button>
            <Button x:Name="CancelButton" Click="CancelButton_Click">Cancel</Button>
        </StackPanel>
    </StackPanel>
</Window>
